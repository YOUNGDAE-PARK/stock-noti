# 📜 Stock-Noti Engineering Standards & Refactoring Mandates

이 파일은 시스템 리팩토링 및 기능 확장 시 반드시 준수해야 하는 **절대 지침**입니다. 시스템의 안정성과 데이터 무결성을 보장하기 위해 다음 수칙을 위반하지 마십시오.

## 1. AI 로직 및 프롬프트 보존 (Core Analytical Integrity)
*   **원본 보존**: 오랜 시간 검증된 AI 프롬프트 텍스트는 리팩토링 과정에서 절대로 임의로 단순화하거나 페르소나를 변경하지 않는다.
*   **변경 금지**: 프롬프트 수정이 필요할 경우 반드시 사용자에게 먼저 제안하고 승인을 득해야 한다.
*   **데이터 규격 유지**: AI가 기대하는 입력 데이터(Input Feed)의 규격을 임의로 제거하거나 순서를 바꾸지 않는다.

## 2. 인터페이스 및 하위 호환성 (Interface Resilience)
*   **인자 유연화 (Polymorphism)**: 함수 인자 타입을 변경(예: String -> Object)할 경우, 이전 방식의 호출도 처리할 수 있는 **방어 코드(Hardening)**를 반드시 포함한다.
*   **전수 조사**: 함수 시그니처 변경 시 해당 함수를 호출하는 모든 지점(Controller, Cron, Test)을 전수 조사하여 수정한다.

## 3. 안정성 및 타임아웃 (System Stability)
*   **I/O 타임아웃 필수**: Gemini API, Slack Webhook, Naver API 등 모든 외부 통신에는 **최대 20초의 타임아웃**을 강제 설정하여 시스템 프리징을 방지한다.
*   **에러 가시성**: `catch` 블록에서 에러를 무시하지 말고, 반드시 구체적인 원인을 로그로 남기거나 리포트에 표시한다.

## 4. 실행 가시성 및 로그 (Observability)
*   **배치 작업 추적**: 주간 배치나 일일 리포트 등 실행 시간이 긴 작업은 반드시 단계별(`STEP 1, 2, 3...`) 진행 상황을 로그로 출력한다.
*   **실행 통계 포함**: 모든 자동 생성 리포트 하단에 분석 소요 시간, 데이터 처리 건수 등 **실행 통계**를 포함하여 데이터 누락 여부를 사용자가 즉시 확인할 수 있게 한다.

## 5. 데이터 격리 및 보안 (Multi-tenancy Security)
*   **사용자 컨텍스트 보장**: 모든 DB 접근 시 `req.user.uid`를 명확히 바인딩하여 데이터가 섞이는 일을 원천 차단한다.
*   **경로 탐색 방지**: 파일 접근(Reports) 시 경로 탐색 공격 방지 로직을 항상 적용한다.

---
*위 지침은 시스템의 품질 유지를 위한 강제 조항이며, 모든 코드 수정 시 이 기준에 맞춰 검증을 완료해야 합니다.*
